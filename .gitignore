node_modules/
dist/
.engine-dj-mcp-test/
*.tgz

# superpowers artefacts stay local
docs/superpowers/
.superpowers/

# local dev MCP wiring — points at ./dist and enables writes; not for anyone else
.mcp.json

# Agent instructions live in the private governance repository (symlinked, never committed)
AGENTS.md
CLAUDE.md
CLAUDE.local.md
